# sequential read
このプロジェクトでは`mmap()`システムコールを用いて、メモリからファイルにアクセスする手法と、`read()`システムコールを用いて、カーネルのバッファを経由してディスクからファイルにアクセスする手法の二つで`time`コマンドからユーザ空間での実行時間、カーネル空間での実行時間の取得を行います

シバンを適用したいなら、`run.sh`の1行目の「#」を1つ削除してください

## 実行方法
```bash
$ chmod u+x run.sh
$ ./run.sh
```
